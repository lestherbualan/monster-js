document state setter
replace BaseComponent documentation to new implementation
remove bind decorator documentation
document detect changes function
document reactivity
document change detection loop guard
document the change in polyfill
implement and document props in custom element
implement a way to check if the 'is' attribute of an element has a corresponding component registered in the module
    - possible way is to add an "isComponent('custom-button', this, element)" function during jsx transformation
add implementation and documentation to define component using 'defineGlobalComponent' function
    - add a way to auto define custom element too.


TEST DOTNET EXECUTION TIME
==============================================
var watch = System.Diagnostics.Stopwatch.StartNew();
// codes to test here
watch.Stop();
Console.WriteLine(watch.ElapsedMilliseconds);